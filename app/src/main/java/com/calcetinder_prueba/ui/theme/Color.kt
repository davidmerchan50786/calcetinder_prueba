package com.calcetinder_prueba.ui.theme

import androidx.compose.ui.graphics.Color

// Paleta inspirada en Tinder: llama rojo → naranja
val CalcetinderPink      = Color(0xFFFF4458)  // Tinder signature red
val CalcetinderPinkLight = Color(0xFFFFF0F0)  // Tint muy claro para contenedores
val CalcetinderPinkDark  = Color(0xFFDD2438)  // Rojo oscuro para estados pressed
val CalcetinderOrange    = Color(0xFFFF6B35)  // Extremo naranja del gradiente llama
val CalcetinderDark      = Color(0xFF111827)
val CalcetinderGray      = Color(0xFF6B7280)
val CalcetinderWhite     = Color(0xFFFFFFFF)

// Colores de acción en las tarjetas de swipe
val LikeGreen = Color(0xFF00C853)  // Verde más vivo (estilo Tinder)
val NopeRed   = Color(0xFFFF4458)  // Mismo rojo que el primario (coherente con Tinder)
